# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# env files (can opt-in for committing if needed)
.env*

# NOTE: .npmrc is deliberately TRACKED. It holds project registry config and no
# secrets — auth lives in ~/.npmrc (npm login) or CI env. Never add a token to it.

# turbo
.turbo

# next
.next

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# typescript
*.tsbuildinfo
next-env.d.ts

# working files
.files
# npm pack output
*.tgz
