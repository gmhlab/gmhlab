/**
 * GW Center for Global Mental Health page content.
 *
 * These are the Center's own records — its portfolio, its bibliography, its
 * innovations. They live in the app rather than in `@gmhlab/blocks` because
 * that package is a published design system: the blocks are record-driven
 * *renderers*, and one organization's content is not library code.
 *
 * Each module documents its own provenance. Read that header before editing
 * the records — it is the spec, and it carries detail the app does not repeat.
 */

export * from "./innovation-detail-data";
export * from "./innovations-data";
export * from "./project-detail-data";
export * from "./projects-data";
export * from "./publications-data";
